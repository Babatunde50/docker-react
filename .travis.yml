sudo: required 
services: 
    - docker 

before_install: 
    - docker build -t babatundeola/docker-react -f Dockerfile.dev . 

script:
    - docker run -e CI=true babatundeola/docker-react npm run test -- --coverage